/// Placeholder binary for contract deployment tooling.
///
/// This binary is not used on-chain. It exists so that Soroban CLI
/// (`soroban contract deploy`) can locate the WASM artifact associated
/// with the membership_token crate.
fn main() {
    println!("Membership Token Contract Binary");
}
